{
  "scripts": {
    "bare-test": "mocha ./bld/test/*.js -t 5000",
    "build": "rimraf bld && tsc --project src/tsconfig.json",
    "test": "yarn build && yarn bare-test"
  },
  "dependencies": {
    "@typescript-eslint/experimental-utils": "^2.7.0",
    "@typescript-eslint/parser": "^2.7.0",
    "typescript": "^3.7.2"
  },
  "devDependencies": {
    "@types/eslint": "^6.1.3",
    "@types/node": "^12.12.7",
    "eslint": "^6.6.0",
    "mocha": "^6.2.2",
    "rimraf": "^3.0.0",
    "ts-node": "^8.5.2"
  }
}
